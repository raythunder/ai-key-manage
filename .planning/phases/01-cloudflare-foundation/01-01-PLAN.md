---
phase: 01-cloudflare-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - open-next.config.ts
autonomous: true
requirements:
  - CF-01
user_setup: []
must_haves:
  truths:
    - "项目具备 Cloudflare 官方推荐的 Next.js Worker 构建入口"
    - "仓库能通过统一脚本执行预览、部署和 Cloudflare env 类型生成"
  artifacts:
    - path: "package.json"
      provides: "Cloudflare preview/deploy/typegen scripts and required dependencies"
      contains: "opennextjs-cloudflare"
    - path: "open-next.config.ts"
      provides: "OpenNext Cloudflare adapter configuration"
      contains: "defineCloudflareConfig"
    - path: "next.config.ts"
      provides: "Next.js config compatible with existing app and Cloudflare workflow"
      min_lines: 8
  key_links:
    - from: "package.json"
      to: "open-next.config.ts"
      via: "preview/deploy build flow"
      pattern: "opennextjs-cloudflare"
---

<objective>
建立现有项目的 Cloudflare Workers 构建基础。

Purpose: 先把现有 Next.js 项目切到官方推荐的 OpenNext + Wrangler 工作流，避免后续 D1 和 Secret 接入建立在错误的部署底座上。
Output: Cloudflare 相关依赖、脚本和 OpenNext 配置文件齐备。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cloudflare-foundation/01-CONTEXT.md
@.planning/phases/01-cloudflare-foundation/01-RESEARCH.md
@package.json
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接入 Cloudflare 构建依赖与脚本</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, .planning/phases/01-cloudflare-foundation/01-RESEARCH.md</read_first>
  <action>在 `package.json` 中新增 `@opennextjs/cloudflare` 依赖和 `wrangler` 开发依赖，版本分别使用已确认可用的最新版本；新增 `preview`、`deploy`、`cf-typegen` 三个脚本，命令分别为 `opennextjs-cloudflare build && opennextjs-cloudflare preview`、`opennextjs-cloudflare build && opennextjs-cloudflare deploy`、`wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts`。保留现有 `dev`、`build`、`start`、`lint` 脚本，不删除任何旧脚本。</action>
  <verify>npm install && npm run lint</verify>
  <acceptance_criteria>
    - `package.json` contains `"preview": "opennextjs-cloudflare build && opennextjs-cloudflare preview"`
    - `package.json` contains `"deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy"`
    - `package.json` contains `"cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts"`
    - `package.json` contains `"@opennextjs/cloudflare"`
    - `package.json` contains `"wrangler"`
  </acceptance_criteria>
  <done>Cloudflare 构建与预览脚本已加入项目且 lint 通过。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 OpenNext 配置并保持 Next 基线稳定</name>
  <files>open-next.config.ts, next.config.ts</files>
  <read_first>next.config.ts, .planning/phases/01-cloudflare-foundation/01-CONTEXT.md</read_first>
  <action>新增 `open-next.config.ts`，内容使用 `@opennextjs/cloudflare` 的 `defineCloudflareConfig()` 默认导出；检查 `next.config.ts`，仅做最小必要调整以兼容新工作流，保留现有 `turbopack.root` 行为，不引入与本阶段无关的实验配置，也不删除当前已有配置。</action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `open-next.config.ts` contains `defineCloudflareConfig`
    - `open-next.config.ts` contains `export default`
    - `next.config.ts` contains `turbopack`
    - `next.config.ts` does not remove `root: projectRoot`
  </acceptance_criteria>
  <done>OpenNext 配置已存在，Next 配置仍保持当前项目可用基线。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `package.json` 和 `open-next.config.ts` 已包含 Cloudflare 工作流入口
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 项目已具备 Cloudflare 预览和部署命令入口
</success_criteria>

<output>
After completion, create `.planning/phases/01-cloudflare-foundation/01-01-SUMMARY.md`
</output>
