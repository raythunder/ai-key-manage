---
phase: 01-cloudflare-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - db/d1/001_initial.sql
  - .gitignore
  - .dev.vars.example
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup:
  - "根据 `.dev.vars.example` 在本地创建实际 `.dev.vars` 文件"
  - "执行 `wrangler d1 execute <database-name> --local --file=./db/d1/001_initial.sql` 初始化本地数据库"
must_haves:
  truths:
    - "项目已有可执行的 D1 初始表结构"
    - "本地开发密码与其他 Secret 有明确且不会入库的放置约定"
    - "首版 D1 结构足以承接当前配置对象的基础字段和嵌套结果字段"
  artifacts:
    - path: "db/d1/001_initial.sql"
      provides: "Initial D1 schema for key configuration persistence"
      contains: "CREATE TABLE"
    - path: ".dev.vars.example"
      provides: "Local secret contract for Cloudflare development"
      contains: "ACCESS_PASSWORD="
    - path: ".gitignore"
      provides: "Ignore rules for local secret files"
      contains: ".dev.vars"
  key_links:
    - from: ".dev.vars.example"
      to: "lib/server/runtime-config.ts"
      via: "ACCESS_PASSWORD local secret contract"
      pattern: "ACCESS_PASSWORD="
    - from: "db/d1/001_initial.sql"
      to: "app/page.tsx"
      via: "schema mirrors current KeyConfig structure"
      pattern: "probe|last_test|benchmarks|base_url|api_key"
---

<objective>
补齐 D1 首版 schema 和本地 Secret 约定。

Purpose: 为后续真正的数据迁移打底，同时让本地预览和远程初始化的基础资产齐备。
Output: D1 初始化 SQL、本地 Secret 示例文件、忽略规则补充。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cloudflare-foundation/01-CONTEXT.md
@.planning/phases/01-cloudflare-foundation/01-RESEARCH.md
@app/page.tsx
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 D1 首版初始化 SQL</name>
  <files>db/d1/001_initial.sql</files>
  <read_first>app/page.tsx, .planning/phases/01-cloudflare-foundation/01-RESEARCH.md</read_first>
  <action>新增 `db/d1/001_initial.sql`，创建一个承接当前配置对象的主表，表中至少包含 `id`、`name`、`base_url`、`api_key`、`model`、`created_at`、`source_meta_json`、`probe_json`、`last_test_json`、`benchmarks_json` 这些列；优先使用简单稳定的 SQLite 兼容定义，不增加拆表、外键或额外索引，除非确实为主键所必需。</action>
  <verify>test -f db/d1/001_initial.sql && rg -n 'CREATE TABLE|base_url|api_key|probe_json|benchmarks_json' db/d1/001_initial.sql</verify>
  <acceptance_criteria>
    - `db/d1/001_initial.sql` contains `CREATE TABLE`
    - `db/d1/001_initial.sql` contains `base_url`
    - `db/d1/001_initial.sql` contains `api_key`
    - `db/d1/001_initial.sql` contains `probe_json`
    - `db/d1/001_initial.sql` contains `last_test_json`
    - `db/d1/001_initial.sql` contains `benchmarks_json`
  </acceptance_criteria>
  <done>D1 首版表结构已存在，字段足以承接当前配置对象。</done>
</task>

<task type="auto">
  <name>Task 2: 建立本地 Secret 示例和忽略规则</name>
  <files>.dev.vars.example, .gitignore</files>
  <read_first>.gitignore, .planning/phases/01-cloudflare-foundation/01-CONTEXT.md</read_first>
  <action>新增 `.dev.vars.example`，至少给出 `ACCESS_PASSWORD=` 的示例键，并根据实际需要给出 D1 或其他本地开发所需占位说明；更新 `.gitignore`，明确忽略 `.dev.vars` 和 `.dev.vars.*`，同时不要误伤 `.dev.vars.example` 这种示例文件。</action>
  <verify>test -f .dev.vars.example && rg -n 'ACCESS_PASSWORD=' .dev.vars.example && rg -n '\\.dev\\.vars' .gitignore</verify>
  <acceptance_criteria>
    - `.dev.vars.example` contains `ACCESS_PASSWORD=`
    - `.gitignore` contains `.dev.vars`
    - `.gitignore` contains `.dev.vars.*`
    - `.gitignore` does not ignore `.dev.vars.example` via an exact entry
  </acceptance_criteria>
  <done>本地 Secret 约定已明确，实际秘钥文件不会进仓库。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f db/d1/001_initial.sql`
- [ ] `test -f .dev.vars.example`
- [ ] `.gitignore` 已覆盖本地 Secret 文件
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 项目已有可执行的 D1 初始结构和本地 Secret 约定
</success_criteria>

<output>
After completion, create `.planning/phases/01-cloudflare-foundation/01-03-SUMMARY.md`
</output>
