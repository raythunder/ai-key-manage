---
phase: 01-cloudflare-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - wrangler.jsonc
  - lib/server/runtime-config.ts
  - app/api/system/bootstrap/route.ts
  - cloudflare-env.d.ts
autonomous: true
requirements:
  - AUTH-02
  - CF-02
user_setup:
  - "登录 Cloudflare 或确保本机 `wrangler whoami` 已通过"
  - "创建 D1 数据库并把真实 `database_id` 填入 `wrangler.jsonc`"
  - "使用 `wrangler secret put ACCESS_PASSWORD` 或本地 `.dev.vars` 提供密码"
must_haves:
  truths:
    - "Worker 运行时可以读取 D1 binding"
    - "Worker 运行时可以读取访问密码 Secret"
    - "项目存在一个不泄露密码内容的基础状态面用于验证平台绑定是否生效"
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Workers runtime config with assets, node compatibility, and D1 binding"
      contains: "\"d1_databases\""
    - path: "lib/server/runtime-config.ts"
      provides: "Centralized runtime access for D1 binding and access password"
      min_lines: 20
    - path: "app/api/system/bootstrap/route.ts"
      provides: "Sanitized bootstrap status endpoint"
      exports: ["GET"]
    - path: "cloudflare-env.d.ts"
      provides: "Generated Cloudflare env typing"
  key_links:
    - from: "app/api/system/bootstrap/route.ts"
      to: "lib/server/runtime-config.ts"
      via: "imported bootstrap status helper"
      pattern: "runtime-config"
    - from: "lib/server/runtime-config.ts"
      to: "wrangler.jsonc"
      via: "typed binding contract"
      pattern: "D1Database|ACCESS_PASSWORD"
---

<objective>
建立 Cloudflare 绑定和运行时配置读取入口。

Purpose: 让 Worker 真正具备读 D1 和访问密码 Secret 的能力，同时保持现有业务 API 不被提前改造。
Output: `wrangler.jsonc`、运行时配置 helper、基础状态路由、Cloudflare env 类型文件。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cloudflare-foundation/01-CONTEXT.md
@.planning/phases/01-cloudflare-foundation/01-RESEARCH.md
@package.json
@app/api/openai/test/route.ts
@app/api/openai/probe/route.ts
@app/api/openai/benchmark/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 Wrangler 配置并声明 D1 绑定</name>
  <files>wrangler.jsonc</files>
  <read_first>package.json, .planning/phases/01-cloudflare-foundation/01-RESEARCH.md</read_first>
  <action>新增 `wrangler.jsonc`，包含 `$schema`、`main: ".open-next/worker.js"`、`assets.directory: ".open-next/assets"`、`assets.binding: "ASSETS"`、`compatibility_date: "2026-04-08"`、`compatibility_flags: ["nodejs_compat"]`、`observability.enabled: true`，并声明一个 D1 数据库绑定，绑定名使用 `DB`，数据库名占位使用项目可读名称，`database_id` 留为明确待填写占位值。不要把访问密码写进 `vars`。</action>
  <verify>test -f wrangler.jsonc && rg -n '\"d1_databases\"|\"nodejs_compat\"|\".open-next/worker.js\"' wrangler.jsonc</verify>
  <acceptance_criteria>
    - `wrangler.jsonc` contains `".open-next/worker.js"`
    - `wrangler.jsonc` contains `".open-next/assets"`
    - `wrangler.jsonc` contains `"nodejs_compat"`
    - `wrangler.jsonc` contains `"d1_databases"`
    - `wrangler.jsonc` contains `"binding": "DB"`
    - `wrangler.jsonc` does not contain `ACCESS_PASSWORD` under `"vars"`
  </acceptance_criteria>
  <done>Wrangler 配置已建立，D1 绑定已声明，未把密码写入明文 vars。</done>
</task>

<task type="auto">
  <name>Task 2: 建立统一运行时配置 helper</name>
  <files>lib/server/runtime-config.ts</files>
  <read_first>wrangler.jsonc, app/api/openai/test/route.ts, app/api/openai/probe/route.ts, app/api/openai/benchmark/route.ts</read_first>
  <action>新增 `lib/server/runtime-config.ts`，集中提供 Cloudflare 运行时读取方法：读取 D1 binding、读取 `ACCESS_PASSWORD` Secret，并提供不会泄露密码内容的状态判断。实现时优先兼容 Cloudflare Worker 运行时，必要时可为本地非 Cloudflare 场景提供安全降级，但不要改变现有 openai 路由行为，也不要在此文件中实现完整鉴权逻辑。</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - `lib/server/runtime-config.ts` contains `ACCESS_PASSWORD`
    - `lib/server/runtime-config.ts` contains `DB`
    - `lib/server/runtime-config.ts` contains a function named `getBootstrapStatus` or `getRuntimeConfig`
    - `lib/server/runtime-config.ts` does not return the raw password in bootstrap status
  </acceptance_criteria>
  <done>Worker 运行时配置读取已集中到单一 helper，且不泄露原始密码。</done>
</task>

<task type="auto">
  <name>Task 3: 提供基础状态路由并生成 Cloudflare env 类型</name>
  <files>app/api/system/bootstrap/route.ts, cloudflare-env.d.ts</files>
  <read_first>lib/server/runtime-config.ts, app/api/openai/test/route.ts, package.json</read_first>
  <action>新增 `app/api/system/bootstrap/route.ts`，导出 `GET`，返回仅包含布尔状态和必要元信息的 JSON，例如是否检测到 D1 绑定、是否检测到访问密码、当前是否在 Cloudflare 运行时；不要返回原始密码或数据库敏感值。随后执行 `npm run cf-typegen` 生成 `cloudflare-env.d.ts`，并确保类型文件与 `DB`、`ACCESS_PASSWORD` 契约一致。</action>
  <verify>npm run cf-typegen && npm run lint</verify>
  <acceptance_criteria>
    - `app/api/system/bootstrap/route.ts` contains `export async function GET`
    - `app/api/system/bootstrap/route.ts` contains `hasAccessPassword`
    - `app/api/system/bootstrap/route.ts` contains `hasDatabase`
    - `app/api/system/bootstrap/route.ts` does not contain a response field exposing the raw password
    - `cloudflare-env.d.ts` exists
    - `cloudflare-env.d.ts` contains `interface CloudflareEnv`
  </acceptance_criteria>
  <done>已存在可验证平台绑定的安全状态路由，Cloudflare env 类型文件已生成。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run cf-typegen`
- [ ] `npm run lint`
- [ ] `wrangler.jsonc`、运行时 helper、基础状态路由三者已连上
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Worker 可以在不改业务主流程的前提下读取 D1 binding 和 Secret
</success_criteria>

<output>
After completion, create `.planning/phases/01-cloudflare-foundation/01-02-SUMMARY.md`
</output>
